{
  "name": "pagermonitor-client",
  "version": "2.5.0",
  "description": "PagerMonitor SDR client — runs on RPi, forwards decoded messages to server",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev":   "node --watch src/index.js"
  },
  "dependencies": {
    "iconv-lite": "^0.6.3",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
